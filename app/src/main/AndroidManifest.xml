<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.info420.fabien.androidtravailpratique">

  <application
    android:name=".common.TaskerApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    
    <provider
      android:authorities="net.info420.fabien.androidtravailpratique.contentprovider"
      android:name=".contentprovider.TaskerContentProvider" />

    <service android:name=".utils.TimeService"/>

    <!-- Afin de modifier la locale (langue) dans l'application, je dois ajouter android:configChanges="locale|layoutDirection" -->
    <!-- locale est assez clair. Pas besoin d'explication, j'imagine. -->
    <!-- Pour ce qui est de layoutDirection, voici pourquoi il faut l'ajouter : http://stackoverflow.com/questions/13856229/onconfigurationchanged-is-not-called-over-jellybean4-2-1 -->

    <activity android:name=".common.MainActivity"
              android:theme="@style/AppTheme.NoActionBar"
              android:configChanges="locale|layoutDirection|orientation|keyboardHidden|screenSize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    
    <activity
      android:name=".common.TaskActivity"
      android:label="@string/title_task"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
    
    <activity
      android:name=".common.EditTaskActivity"
      android:label="@string/title_activity_edit_task"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
    
    <activity
      android:name=".common.NewTaskActivity"
      android:label="@string/title_activity_new_task"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>

    <!-- TODO : Pourquoi j'essaie de faire passé un fragment en activité?? -->
    <activity
      android:name=".common.EmployeeListFragment"
      android:label="@string/title_employee_list"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
    
    <activity
      android:name=".common.EditEmployeeActivity"
      android:label="@string/title_activity_edit_employee"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
    
    <activity
      android:name=".common.NewEmployeeActivity"
      android:label="@string/title_activity_new_employee"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
    
    <activity
      android:name=".common.EmployeeActivity"
      android:label="@string/title_activity_employee"
      android:theme="@style/AppTheme.NoActionBar"
      android:configChanges="locale|layoutDirection">
    </activity>
  </application>

</manifest>